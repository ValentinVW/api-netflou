{
  "name": "api-netfou-prog",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^20.6.1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
